Producer와 Comsumer는 Kafka에게 메시지를 발행하고 소비하는 Client

Producer 와 Consumer는 서로의 존재를 알지 못하며,
각각의 고유의 속도로 Commit Log에 Write/Read를 수행한다.
# Producer
메시지를 생산하여 Kafka의 Topic으로 전달하는 애플리케이션


# Record(Message)의 구조

![[Pasted image 20251230171616.png]]

Kafka는 Record(데이터)를 Serializer를 통해 Byte Array로 저장 후 
Consume 시점에 Deserializer를 통해 역직렬화 후 사용
![[Pasted image 20251230171928.png]]


# Producing to Kafka (High-Level)

![[Pasted image 20251230173928.png]]

- Producer Record
	Topic과 Value는 필수데이터
- Serializer
	Kafka에게 바이트 배열로 전달하기 위해 직렬화를 수행
- Partitioner
	파티션에 대한 지정 가능
- Compress (Optional)
	압축옵션 설정
- RecordAccmulator
	파티셔너에 의해 결정된 파티션에서 묶음으로 Producer에게 Bulk 전달(옵션으로 설정 가능)

## Partitioner
![[Pasted image 20251230174040.png]]

Partitioner는 해당 토픽의 파티션 갯수를 알고있다.
Key값을 지정한 경우 특정 알고리즘에 의해 파티션을 지정하여 전달

![[Pasted image 20251230174233.png]]
Partitioner는 개발하여 교체 가능하다.

- Message == Record == Event == Data
- Message 는 Header와 Key 그리고 Value로 구성
- Kafka는 Record(데이터)를 Byte Array로 저장
- Producer는 Serializer, Consumer는 Desirializer를 사용
- Producer는 Message의 Key 존재 여부에 따라 Partitioner를 통한 메시지 처리 방식이 다르다.

# Producer Acks
acks 는 Producer가 메시지 발행 요청이 성공할 때를 정의하는데 사용되는 파라미터이다.

### acks=0
Kafka Partition에 써졌는지 응답을 받지 않고, 발행 후 결과에 상관없이 성공으로 간주.
손실이 발생할 수 있지만, 가장 빠르게 메세지를 보낼 수 있다.
### acks=1 (defalut)
Leader에게 메세지가 전달 되면 Kafka가 성공 응답을 보낸다.
Follower들에게는 복제 되었는지 확인하지 않으며,
Follower에게 복제 전 장애 시 유실 가증성.

`At Most Once(최대 한번)`를 보장한다
### acks=all (acks=-1 과 동일)
모든 ISR들에게 복제 되었을 때 성공 메세지를 받는다.
레코드 유실이 가장 낮지만 성능이 가장 낮다.

`At least Once(최소 한번)`를 보장한다

| 설정 값     | 설명        | 장점               | 단점            | 사용 시나리오                             |
| -------- | --------- | ---------------- | ------------- | ----------------------------------- |
| acks=0   | 승인 없음     | 가장 빠름            | 메시지 유실 가능성 높음 | 성능이 중요하고, 데이터 유실이 허용되는 경우 (로그 수집 등) |
| acks=1   | 리더 승인     | 적당한 성능           | 메시지 유실 가능성 있음 | 일반적인 메시징 시스템                        |
| acks=all | 모든 브로커 승인 | 메시지 유실 가능성 거의 없음 | 가장 느림         | 데이터 유실이 절대 허용되지 않는 중요한 데이터 처리       |

## Producer Retry
네트워크 또는 시스템의 일시적인 오류를 보완하기 위해 설정

| 설정 값                | 설명                            | Default     |
| ------------------- | ----------------------------- | ----------- |
| retries             | 메시지 send 재시도 횟수               | MAX_INT     |
| retry.backoff.ms    | 재시도 사이 시간                     | 100         |
| request.timeout.ms  | Producer가 kafka로부터 응답 대기하는 시간 | 30,000(30초) |
| delivery.timeout.ms | send() 후 성공/실패를 보고하는 시간의 상한   | 120,000(2분) |
![[Pasted image 20260102173216.png]]
# Producer Batch
RPC(Remote Procedure Call) 수를 줄여 Broker가 처리하는 작업이 줄어들기 때문
(통신 오버헤드 제거)
![[Pasted image 20260102173046.png]]

