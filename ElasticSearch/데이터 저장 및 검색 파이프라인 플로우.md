

문서 요청
   ↓
메모리 버퍼(Buffer)에 기록
   ↓
Translog에 로그 기록 (디스크)
   ↓
Refresh → 검색 가능하게 됨
   ↓
Flush → 세그먼트 디스크 반영, Translog 초기화

순서로 색인이 완료

- `translog`는 **안전한 복구용 로그**
- `refresh`는 **검색 가능하게 만드는 과정**
- `flush`는 **디스크에 완전히 반영하고 로그를 비우는 과정**
